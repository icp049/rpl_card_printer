# -*- mode: python ; coding: utf-8 -*-


a = Analysis(
    ['rpl_card_printer_network.py'],
    pathex=[],
    binaries=[],
    datas=[('printer.ico', '.'), ('snip1.PNG', '.'), ('snip2.PNG', '.')],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
    optimize=0,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.datas,
    [],
    name='rpl_card_printer_network',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=False,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
    icon=['printer.ico'],
)
